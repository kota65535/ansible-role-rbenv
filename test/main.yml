---
# Run the following command for test.
#
#   cd test
#   ansible-playbook main.yml -i inventory
#
- hosts: all
  vars:
    rbenv_ruby_versions:
      - 1.9.1
      - 2.3.x
    rbenv_users:
      - test01
      - test02
  roles:
    - kota65535.rbenv
